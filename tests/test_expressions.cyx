// Test of precedence of mathematical and logical operations.

program() {
    int a;
    int b;
    float result;
    boolean test;

    a = 10;
    b = 3;

    result = (a + b) * 2 - (a / b);
    test = (a > b) && (result != 0);

    printf("Result: %.2f\n", result);
    printf("Test: %d\n", test);
}
