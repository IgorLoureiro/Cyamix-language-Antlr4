// Test a simple program

program() {
  int a, b = 1, c = 3;

  printf("Number B == %d && Number C == %d \n", b, c);

  text fullname = "John Doe";
  printf("name: %s\n", fullname);

  int age = 20;
  printf("age: %d\n", age);

  if (c > 1) {
    printf("ok\n");
  } else {
    printf("fail\n");
  }

}
