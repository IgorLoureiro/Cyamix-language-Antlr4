// Validation of for-structure with increment and arithmetic operation.

program() {
    int i, sum = 0;

    for (i = 0; i < 5; i = i + 1) {
        sum = sum + i;
    }

    printf("Total sum: %d", sum);
}
