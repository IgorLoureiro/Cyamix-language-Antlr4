// Test Valid declarations and assignments with different types

program() {
    int age;
    float height;
    char genre;
    boolean active;

    text name = "John Doe";
    age = 25;
    height = 1.72;
    genre = 'M';
    active = 1 != 1;

    // Test with multi declaration of variables
    int a, b = 1, c = 2;

    printf("Name: %s\n", name);
    printf("Age: %d\n", age);
    printf("Height: %.2f\n", height);
    printf("Genre: %c\n", genre);
    printf("Active: %d\n", active);
}
